---
import type { Locale } from '../lib/i18n'

interface Props {
  lang: Locale
}

const { lang } = Astro.props
const base = import.meta.env.BASE_URL

const enUrl = `${base}en/`
const deUrl = `${base}de/`
const isEnglish = lang === 'en'
---

<header class="px-4 py-5 md:py-7">
  <div class="container mx-auto">
    <div class="mx-auto max-w-[1230px]">
      <div class="flex items-center justify-between">
        <!-- Logo -->
        <div>
          <a href={`${base}${lang}/`}>
            <img
              class="w-32"
              src={`${base}images/logo/logo.webp`}
              srcset={`${base}images/logo/logo-sm.webp 320w, ${base}images/logo/logo.webp 560w`}
              sizes="128px"
              alt="GoRehab Logo"
              width="560"
              height="177"
            />
          </a>
        </div>

        <!-- Language Switcher -->
        <nav aria-label={isEnglish ? 'Language' : 'Sprache'}>
          <div class="flex items-center space-x-2 text-base font-medium">
            <a
              href={deUrl}
              aria-current={!isEnglish ? 'page' : undefined}
              class:list={['transition-colors', !isEnglish ? 'text-green font-bold' : 'hover:text-green text-gray-600']}
            >
              DE
            </a>
            <span class="text-gray-400">|</span>
            <a
              href={enUrl}
              aria-current={isEnglish ? 'page' : undefined}
              class:list={['transition-colors', isEnglish ? 'text-green font-bold' : 'hover:text-green text-gray-600']}
            >
              EN
            </a>
          </div>
        </nav>
      </div>
    </div>
  </div>
</header>
